<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.luyuni.mybatis.dao.UserMapper">
    <select id="findByName" resultType="top.luyuni.mybatis.pojo.User">
        SELECT * FROM user WHERE  name = #{name}
    </select>
    <select id="findAllUsers" resultType="top.luyuni.mybatis.pojo.User">
        SELECT * FROM user
    </select>
</mapper>
